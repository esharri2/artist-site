{{>navbar }}

<div class="container paintings">
  {{#each paintings}}
  <div class="column">
    {{#each this}}
    <img src={{image.secure_url}} alt={{title}} />
    {{/each}}
  </div>
  {{/each}}
</div>